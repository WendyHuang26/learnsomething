#ifndef PRICINGUTIL_H
#define PRICINGUTIL_H

#include <vector>

std::vector<bool> primeNumList(int n);
std::vector<int> prime_factors(int n, const std::vector<bool>& prime_list);
void optimizedCode();

#endif // PRICINGUTIL_H
